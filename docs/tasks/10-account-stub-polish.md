---
id: 10
title: Полировка раздела «Аккаунт» (без бэкенда)
status: todo
agent: frontend-builder + commerce-logic
priority: P3
created: 2026-05-25
updated: 2026-05-25
---

## Цель
Превратить пустую страницу `/account` (сейчас stub) в полезный раздел: история заказов из localStorage, "избранное" (mock), редактируемый профиль (имя/контакты). Без реального auth и бэкенда — всё клиентское.

## Контекст
- Сейчас `components/AccountView.tsx` существует, но контента нет (или есть stub).
- Зависимость: задача 07 должна быть выполнена — заказы будут лежать в `paper-fairies-orders-v1`.
- Профиль хранить отдельно: `paper-fairies-profile-v1`.
- Избранное: `paper-fairies-favorites-v1` (массив `productId`).

## Acceptance criteria
**Часть 1 — commerce-logic:**
- [ ] В `types/index.ts` добавлены типы `UserProfile`, `Favorites`.
- [ ] В `lib/store.ts` (или `lib/account.ts`) — Zustand-стор/функции для profile и favorites с persist (отдельные ключи).
- [ ] API: `useProfile()`, `useFavorites()`, `toggleFavorite(productId)`, `isFavorite(productId)`, `updateProfile(partial)`.
- [ ] Утилита `enrichOrdersWithProducts(orders)` — подтягивает текущие данные товаров по id.

**Часть 2 — frontend-builder:**
- [ ] Страница `/account` — вкладки: "Профиль", "Заказы", "Избранное".
- [ ] Профиль: форма имя/email/телефон, кнопка "Сохранить", сообщение об успешном сохранении.
- [ ] Заказы: список заказов из `getOrders()` (новые сверху), карточка заказа разворачивается с деталями.
- [ ] Избранное: грид карточек товаров, кнопка "Убрать из избранного". Пустое состояние — текст + CTA в каталог.
- [ ] На `ProductCard.tsx` добавить иконку-сердечко (Heart из lucide), переключающую `toggleFavorite` (только если не сломает существующий дизайн).
- [ ] Стилизация в палитре, focus-visible, адаптив.

## Не входит в задачу
- Реальная регистрация/логин (auth).
- Синхронизация между устройствами.
- Удаление аккаунта/экспорт данных (152-ФЗ — если потребуется, отдельная задача).

## Артефакты
_Заполняется по факту._

## Заметки исполнения
_Заполняется по факту._
