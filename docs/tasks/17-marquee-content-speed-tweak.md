---
id: 17
title: Бегущая лента — обновить контент и ускорить
status: done
agent: frontend-builder
priority: P1
created: 2026-05-25
updated: 2026-05-25
---

## Цель
Точечная правка бегущей ленты после задачи 16: обновить содержимое и порядок пунктов, ускорить движение.

## Контекст
Файл: `components/Hero.tsx`, строки 168-180 (вызов `<Marquee>`). Сейчас:
- items: `["Персонализация", "Упаковка подарков", "Доставка"]`
- durationSeconds: 50

## Что сделать
1. **items** заменить на 4 пункта в указанном порядке, все с заглавной буквы:
   ```
   ["Доставка по всей России", "Персонализация", "Подарочные боксы", "Упаковка подарков"]
   ```
2. **durationSeconds**: 50 → 30 (значительно быстрее, при этом не превращается в мельтешение).

Остальные параметры (`itemClassName`, `dividers`, `dividerClassName`, обёртка) — **не трогать**.

## Acceptance criteria
- [x] items в `<Marquee>` — точно те 4 пункта в указанном порядке, с заглавных.
- [x] `durationSeconds={30}`.
- [x] Никаких других правок в Hero.tsx или Marquee.tsx.
- [x] `npx tsc --noEmit` проходит. **НЕ запускать** `npm run build`.

## Не входит в задачу
- Менять Marquee.tsx, обёртку ленты, любые другие секции.

## Артефакты
- `components/Hero.tsx` (строки 170, 174–179): обновлены items на 4 пункта («Доставка по всей России», «Персонализация», «Подарочные боксы», «Упаковка подарков») и `durationSeconds={50}` → `30`.

## Заметки исполнения
Точечная правка бегущей ленты после задачи 16. Замена контента и ускорение в одном коммите. `npx tsc --noEmit` прошёл успешно. `npm run build` не запускался (как указано в AC). Никаких побочных правок в других компонентах.
